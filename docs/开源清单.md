## 安全自查

| 检查项 | 检查方法 | 结果 |
|--------|----------|------|
| `.gitignore` 含 `.env` | 确认 `.env` 未被 Git 跟踪 | ✅ 通过 |
| 密钥扫描（`sk-`/`ghp_`/`AKIA`） | 全项目 `Select-String` 扫描 | ✅ 命中 0 条，密钥均走 `os.getenv` |
| 提交历史无泄露 | 首次公开仓库，无历史 commit 残留 | ✅ 通过 |